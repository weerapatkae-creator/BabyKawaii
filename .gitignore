# ─── Credentials & Local Config ──────────────────────
config/local.php

# ─── Uploads (ไฟล์ที่ user อัปโหลด) ────────────────
uploads/
assets/uploads/products/
assets/uploads/media/

# ─── Claude local settings (มี path เฉพาะเครื่อง) ──
.claude/settings.local.json

# ─── Claude skills: ใช้ใน local เท่านั้น ห้าม commit/push ──
.claude/skills/

# ─── System / IDE ─────────────────────────────────────
.DS_Store
Thumbs.db
*.log
*.bak
*.swp
*.swo

# ─── PHP / Composer ───────────────────────────────────
vendor/
composer.lock

# ─── Node (ถ้ามีในอนาคต) ─────────────────────────────
node_modules/
npm-debug.log*
